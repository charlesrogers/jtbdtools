# Get count of factor levels

Get count of factor levels

## Usage

``` r
get_count(data_frame, col_name)
```

## Arguments

- data_frame:

  A data frame containing the column to be counted

- col_name:

  The name of the column to count

## Value

A data frame with counts for each factor level
