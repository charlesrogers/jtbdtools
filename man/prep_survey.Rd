% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{prep_survey}
\alias{prep_survey}
\title{Prepare survey data for JTBD analysis}
\usage{
prep_survey(
  df,
  imp_cols,
  sat_cols,
  job_steps = NULL,
  objective_names = NULL,
  segment_col = NULL
)
}
\arguments{
\item{df}{A data frame containing survey responses}

\item{imp_cols}{Character vector of importance column names}

\item{sat_cols}{Character vector of satisfaction column names (must be same length and order as \code{imp_cols})}

\item{job_steps}{A named list mapping job step names to column indices or names.
Example: \code{list(researching = 1:4, purchasing = 5:8)} means the first 4 imp/sat
pairs belong to "researching" and the next 4 to "purchasing".
If NULL, all objectives are assigned to a single job step called "all".}

\item{objective_names}{Optional character vector of clean objective names. If NULL,
names are auto-generated from column names by cleaning punctuation and spaces.}

\item{segment_col}{Optional name of a column to keep as segmentation variable.
Can also be a character vector of column names to keep.}
}
\value{
A data frame ready for \code{\link[=get_jtbd_scores]{get_jtbd_scores()}}, with properly named
\code{imp__}/\code{sat__} columns and factor values (1-5)
}
\description{
The universal data prep function. Takes any data frame and renames columns
to the required \code{imp__job_step.objective} / \code{sat__job_step.objective} format.
Works with data from any source (Qualtrics, SurveyMonkey, Google Forms, CSV, etc.).
}
\examples{
sample_file <- system.file("extdata", "sample_qualtrics.csv", package = "jtbdtools")
df <- read_qualtrics(sample_file)
detected <- detect_imp_sat(df)

ready <- prep_survey(df,
  imp_cols = detected$imp,
  sat_cols = detected$sat,
  job_steps = list(researching = 1:4),
  segment_col = "Q3"
)
head(ready)
}
\seealso{
Other import: 
\code{\link{detect_imp_sat}()},
\code{\link{prep_qualtrics}()},
\code{\link{read_qualtrics}()},
\code{\link{validate_jtbd_data}()}
}
\concept{import}
