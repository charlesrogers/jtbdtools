% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{add_score_cis}
\alias{add_score_cis}
\title{Add Wilson confidence intervals to JTBD scores}
\usage{
add_score_cis(scores, n, conf_level = 0.95)
}
\arguments{
\item{scores}{A scores data frame with \verb{imp.<seg>}, \verb{sat.<seg>}, \verb{opp.<seg>} columns.}

\item{n}{Sample size. Either a single integer applied to every segment, or a
named integer vector keyed by segment (e.g. \code{c(all = 200, casual = 80)}).}

\item{conf_level}{Confidence level (default 0.95).}
}
\value{
The input data frame with new \verb{<imp|sat|opp>_lo.<seg>},
\verb{<imp|sat|opp>_hi.<seg>}, and \verb{<imp|sat|opp>_se.<seg>} columns.
}
\description{
Augments a scores data frame from \code{\link[=get_jtbd_scores]{get_jtbd_scores()}} or
\code{\link[=get_jtbd_scores.comparison]{get_jtbd_scores.comparison()}} with confidence-interval bounds for each
importance, satisfaction, and opportunity column. Because raw scores are
top-2-box proportions scaled to 0–10, the imp/sat bounds use the Wilson
interval for binomial proportions. Opportunity bounds propagate via the
delta method on \code{opp = imp + max(0, imp - sat)}.
}
\examples{
data(jtbd_sample)
scores <- get_jtbd_scores(jtbd_sample)
add_score_cis(scores, n = get_sample_size(jtbd_sample))
}
\seealso{
Other scoring: 
\code{\link{calculate_opportunity_score}()},
\code{\link{calculate_pop_pct_score}()},
\code{\link{find_imp_sat_columns}()},
\code{\link{get_jtbd_scores}()},
\code{\link{get_jtbd_scores.individual}()},
\code{\link{get_sample_size}()},
\code{\link{split_imp_sat_columns}()}
}
\concept{scoring}
